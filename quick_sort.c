#include <stdio.h>
void quick_sort(data)
{
	int size = (sizeof(data) / sizeof(int)) / 3;
	int less_then[size];
	int equal[size];
	int more_than[size];
}
int main(void)
{
	int my_list[] = {5, 12, 4, 32, 188, 16, 78, 1, 41, 75};
	return 0;
}
